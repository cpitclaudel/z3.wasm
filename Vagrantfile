# -*- mode: ruby -*-

Vagrant.configure(2) do |config|
  config.vm.box = "ubuntu/xenial64"

  config.vm.provider 'virtualbox' do |vb|
    vb.name = 'Z3'
    vb.memory = '8192'
    vb.cpus = 6
  end
end
